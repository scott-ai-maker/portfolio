import { Container } from '@/components/Container';

export default function TestAliasResolution() {
  return (
    <Container>
      <p>If you see this, the alias resolution for '@/components/Container' is working correctly.</p>
    </Container>
  );
}